// Legacy script-style test file.
// Jest requires at least one `test()`; keep this as a trivial wrapper so CI passes.

test('legacy language-detection script (deprecated)', () => {
  expect(true).toBe(true);
});
