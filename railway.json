{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "github": {
      "repo": "Mdx2025/emailbot"
    }
  },
  "deploy": {
    "startCommand": "npm run server",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
