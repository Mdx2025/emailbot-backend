{
  "$schema": "https://railway.app/schema.json",
  "name": "emailbot-backend",
  "build": {
    "builder": "NIXPACKS",
    "github": {
      "repo": "Mdx2025/emailbot-backend"
    }
  },
  "deploy": {
    "startCommand": "npm run server:simple",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
